defaults:
  - task: uav_combat_obs24
  - _self_

name: train_uav_transformer_combat_obs24_noswitch
task_name: ${task.name}

horizon: 30
n_obs_steps: 24
n_action_steps: 6
n_latency_steps: 0
pred_action_steps_only: True

policy:
  _target_: diffusion_policy.policy.diffusion_transformer_lowdim_policy.DiffusionTransformerLowdimPolicy
  horizon: ${horizon}
  n_obs_steps: ${n_obs_steps}
  n_action_steps: ${n_action_steps}
  obs_dim: ${task.obs_dim}
  action_dim: ${task.action_dim}
  obs_as_cond: True
  pred_action_steps_only: ${pred_action_steps_only}
  velocity_loss_weight: 0.0
  short_horizon_loss_weight: 0.0
  terminal_pos_loss_weight: 0.1
  relative_path_loss_weight: 0.0
  short_horizon_focus_steps: 3
  short_horizon_focus_gain: 3.0
  model:
    _target_: diffusion_policy.model.diffusion.transformer_for_diffusion.TransformerForDiffusion
    input_dim: ${task.action_dim}
    output_dim: ${task.action_dim}
    horizon: ${horizon}
    n_obs_steps: ${n_obs_steps}
    cond_dim: ${task.obs_dim}
    n_layer: 12
    n_head: 8
    n_emb: 512
    p_drop_emb: 0.1
    p_drop_attn: 0.1
    n_cond_layers: 8
    causal_attn: True
    time_as_cond: True
    obs_as_cond: True
  num_inference_steps: 20
  noise_scheduler:
    _target_: diffusers.schedulers.scheduling_ddim.DDIMScheduler
    num_train_timesteps: 100
    beta_start: 0.0001
    beta_end: 0.02
    beta_schedule: linear
    clip_sample: True
    set_alpha_to_one: True
    steps_offset: 0
    prediction_type: epsilon

training:
  device: "cuda:2"
  seed: 32
  debug: False
  resume: False
  num_epochs: 150
  gradient_accumulate_steps: 1
  lr: 2e-5
  lr_scheduler: cosine
  lr_warmup_steps: 1000
  weight_decay: 1.0e-3
  checkpoint_every: 50
  gradient_accumulate_every: 1
  use_ema: True
  rollout_every: 1
  val_every: 1
  sample_every: 5
  max_train_steps: null
  max_val_steps: null
  tqdm_interval_sec: 1.0

optimizer:
  learning_rate: ${training.lr}
  weight_decay: 1.0e-4
  betas: [0.9, 0.95]

ema:
  _target_: diffusion_policy.model.diffusion.ema_model.EMAModel
  update_after_step: 0
  inv_gamma: 1.0
  power: 0.75
  min_value: 0.0
  max_value: 0.9999

logging:
  project: diffusion_policy_debug
  resume: True
  mode: disabled
  name: ${now:%Y.%m.%d-%H.%M.%S}_${name}_${task_name}
  tags: ["${name}", "${task_name}", "combat_obs24", "noswitch"]
  id: null
  group: null

checkpoint:
  topk:
    monitor_key: test_mean_score_path
    mode: max
    k: 5
    format_str: 'epoch={epoch:04d}-test_mean_score_path={test_mean_score_path:.3f}.ckpt'
  save_last_ckpt: True
  save_last_snapshot: False

task:
  name: uav_combat_obs24_noswitch
  dataset:
    zarr_path: ${hydra:runtime.cwd}/data/uav_combat/test_data.zarr
  env_runner:
    multi_sample_eval_samples: 20
    multi_sample_eval_max_batches: 10

dataloader:
  batch_size: 256
  num_workers: 8
  shuffle: True
  pin_memory: True
  persistent_workers: True

val_dataloader:
  batch_size: 256
  num_workers: 8
  shuffle: False
  pin_memory: True
  persistent_workers: True

_target_: diffusion_policy.workspace.train_diffusion_transformer_lowdim_workspace.TrainDiffusionTransformerLowdimWorkspace
